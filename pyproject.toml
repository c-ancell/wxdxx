[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wxdxx"
version = "0.8.3"
description = "A TUI for viewing NWS text products from SPC, WPC, NHC, and local WFOs"
requires-python = ">=3.11"
license = "MIT"
authors = [
    { name = "c-ancell" }
]
keywords = ["weather", "nws", "spc", "wpc", "nhc", "tui", "terminal", "severe-weather"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Atmospheric Science",
]
dependencies = [
    "textual>=0.40.0",
    "httpx>=0.25.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-asyncio>=0.21.0",
    "textual-dev>=1.0.0",
    "ruff>=0.1.0",
]

[project.scripts]
wxdxx = "wxdxx.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/wxdxx"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
